git branch dev
git branch test
git checkout dev
touch file1 file2
git status 
git add .
git commit -m "this is message for dev branc"
git push origin dev
git chechout main
git push origin main

to delete branches 

git push origin :dev
git push origin :test

to delete branches locally

git branch -d

to make a tag

git tag -a v1.4 -m "make tag"
git push origin v1.4

to list tags

git tag

to delete tag

locally
git tag -d v1.4 

from git gui
git push origin --delete v1.4
